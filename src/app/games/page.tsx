import Link from "next/link";
import React from "react";

const page = () => {
    return (
        <div id="games">
            <Link href="/pokedex">Pokedex</Link>
        </div>
    );
};

export default page;
